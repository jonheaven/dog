#!/usr/bin/env bash

set -euxo pipefail

systemctl stop dog

for host in alpha bravo; do
  rsync \
    --archive \
    --progress \
    /var/lib/dog/index.redb \
    root@$host.ordinals.net:/var/lib/dog/index.redb.new
done

systemctl restart dog
