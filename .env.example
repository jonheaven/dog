# dog — Dogecoin ordinals indexer / wallet
# Copy this file to .env and fill in your values.
# All variables are optional unless noted.

# ---------------------------------------------------------------------------
# Dogecoin Core connection
# ---------------------------------------------------------------------------

# Path to the Dogecoin Core data directory (contains blocks/, .cookie, etc.)
# Default (Linux):   ~/.dogecoin
# Default (Windows): %APPDATA%\Dogecoin
DOGECOIN_DATA_DIR=F:\DogecoinData

# RPC endpoint for Dogecoin Core.
# Default: http://127.0.0.1:22555  (mainnet)
BITCOIN_RPC_URL=http://127.0.0.1:22555

# RPC credentials (leave unset to use the .cookie file instead)
BITCOIN_RPC_USERNAME=
BITCOIN_RPC_PASSWORD=

# Maximum number of concurrent RPC requests.  Default: 12
# BITCOIN_RPC_LIMIT=12

# Path to the .cookie file (auto-detected from DOGECOIN_DATA_DIR by default)
# COOKIE_FILE=F:\DogecoinData\.cookie

# ---------------------------------------------------------------------------
# dog index / data storage
# ---------------------------------------------------------------------------

# Where dog stores its index database (index.redb) and the blk-index copy.
# Default: platform data dir / dog / index.redb
# DATA_DIR=C:\Users\you\AppData\Roaming\dog

# Path to the redb index file.
# INDEX=C:\Users\you\AppData\Roaming\dog\index.redb

# How many blocks to process before committing to disk.  Default: 5000
# COMMIT_INTERVAL=5000

# ---------------------------------------------------------------------------
# Indexing options (set to "true" to enable)
# ---------------------------------------------------------------------------

# Index koinu (sat) ranges for each UTXO — needed for sat hunting / rarities.
# INDEX_SATS=false

# Index the address (script pubkey) for every UTXO — needed for wallet queries.
# INDEX_ADDRESSES=false

# Index Dunes (Rune-equivalent on Dogecoin).
# INDEX_RUNES=false

# Index all transactions (larger index, enables tx lookups).
# INDEX_TRANSACTIONS=false

# Disable inscription indexing entirely (saves space if you only need Dunes).
# NO_INDEX_INSCRIPTIONS=false

# ---------------------------------------------------------------------------
# Explorer server (dog server)
# ---------------------------------------------------------------------------

# HTTP port for the built-in block explorer.  Default: 80
# HTTP_PORT=8080

# Server basic-auth credentials (optional)
# SERVER_USERNAME=
# SERVER_PASSWORD=

# Public URL of this server (used for self-referencing links)
# SERVER_URL=http://localhost:8080

# ---------------------------------------------------------------------------
# Advanced / rarely needed
# ---------------------------------------------------------------------------

# Hard stop at this block height (useful for testing).
# HEIGHT_LIMIT=5000000

# Maximum number of reorg savepoints kept on disk.  Default: 10
# MAX_SAVEPOINTS=10

# How often to write a savepoint (in blocks).  Default: 10
# SAVEPOINT_INTERVAL=10

# In-memory cache size for the index (bytes).  Default: auto
# INDEX_CACHE_SIZE=
